# yaml-language-server: $schema=../schema/continue_training.schema.json
# ========================================================
# Continue training configuration
# This mode resumes optimization in the original run directory.
# The original experiment name, model, data, routing, and loss settings
# are reused automatically and cannot be overridden here.
# ========================================================
experiment:
  mode: continue_training
  post_training_inference: true

# ========================================================
# Model loading
# Required: selects the existing run to resume in-place.
# ========================================================
load_model:
  canonical_load: true
  dataset_name: Gag_noisy_timelapses
  subfolder: HDN
  exp_name: HDN_betaKL05_00
  load_method: state_dict
  best_or_last: last

# ========================================================
# Training overrides
# Only include values you want to change for the resumed run.
# ========================================================
training:
  n_epochs: 100
  # learning_rate: 1e-8
  # debug_stop: false
  # warmup:
  #   enabled: false
  #   steps: 500
  #   start_factor: 0.1
  # auto_stop:
  #   enabled: false
  #   metrics: val_loss
  #   patience: 30
  # early_stop: false  # legacy debug-stop alias (deprecated)

# ========================================================
# Saving configuration
# ========================================================
# saving:
#   enabled: true
#   state_dict: true
#   entire_model: true

# ========================================================
# Recovery parameters override
# ========================================================
# recovery:
#   hdn_safe_resume:
#     enabled: true
#     auto_use_safe_checkpoint_on_continue: true
#     drop_optimizer_scheduler_state_on_safe_resume: true
#     rewind_steps: 1
#     lr_scale: 0.9


# ========================================================
# Tensorboard configuration
# ========================================================
# tensorboard:
#   enabled: false
